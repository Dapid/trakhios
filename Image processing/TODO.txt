Hrun:
logic to classes, global variables to class variables

explain things like "tol", "step_tol"
x0 is a list of coordinates, non intuitive 		# I take it as \vec x_0, for me it is. => DOCUMENT


Silenus:
Check existance: refactorize and simplify. Look "access" and "os.exists"

raise "string" deprecated

ask_overwrite reencapsule


Config object

class config(object) :
	def __init__(self) :
		# carga fichero y pon sus datos en el diccionario "self._config"
	def get(self, key) :
		return self._config[key]
